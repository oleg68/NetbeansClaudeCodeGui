{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "filePath": {
      "type": "string",
      "description": "Path to the file to open"
    },
    "preview": {
      "type": "boolean",
      "description": "Whether to open in preview mode"
    },
    "pattern": {
      "type": "string",
      "description": "Optional text to search for and navigate to after opening the file"
    }
  },
  "required": ["filePath"]
}